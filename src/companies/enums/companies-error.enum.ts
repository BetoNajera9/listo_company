export enum CompaniesError {
	SEARCH = 'The search wasn’t successful.',
	CREATE = 'The registry wasn’t successfully saved.',
	UPDATE = 'The registry wasn’t successfully updated.',
	REMOVE = 'The registry wasn’t successfully removed.',
}
